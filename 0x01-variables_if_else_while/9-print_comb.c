#include<stdio.h>
/**
 *main - Entry point
 *Description 'Patience, persistance and perspiration'
 *Return: Always 0
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		putchar(n + '0');
		if (a < 9)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}

